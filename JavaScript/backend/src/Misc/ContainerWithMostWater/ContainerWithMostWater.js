var maxArea = function (height) {}

let input = [
  [1, 8, 6, 2, 5, 4, 8, 3, 7],
  [1, 1],
]
for (let i = 0; i < input.length; i++) {
  let arr = input[i]
  console.log('Max Area: ' + maxArea(arr))
}
